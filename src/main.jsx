import React from "react";
import ReactDOM from "react-dom/client";
import VaultAccess from "./VaultAccess.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <VaultAccess />
  </React.StrictMode>
);
